{
  "name": "liveicons-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "populate": "tsx scripts/populate.ts",
    "generate": "tsx scripts/generate.ts",
    "animate": "tsx scripts/animate.ts",
    "validate": "tsx scripts/validate.ts",
    "check-imports": "tsx scripts/check-imports.ts",
    "check-duplicates": "tsx scripts/check-duplicates.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "turbo": "^2.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  },
  "author": {
    "name": "Vinicius Rocha",
    "email": "work@hayato.me",
    "url": "https://github.com/hayatolab"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
